#ifndef MATRIZ_HPP
#define MATRIZ_HPP

struct Matriz {
    unsigned long long int m[2][2];

    Matriz();
    Matriz(unsigned long long int x1, unsigned long long int x2, unsigned long long int x3, unsigned long long int x4);
};

#endif